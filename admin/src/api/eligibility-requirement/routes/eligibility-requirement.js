'use strict';

/**
 * eligibility-requirement router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::eligibility-requirement.eligibility-requirement');
