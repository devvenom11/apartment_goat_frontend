'use strict';

/**
 * eligibility-requirement controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::eligibility-requirement.eligibility-requirement');
